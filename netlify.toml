[build]
  command = "npm run build" # আপনার বিল্ড কমান্ড, যদি থাকে
  publish = "public"        # public ফোল্ডার সার্ভ করা হবে
  functions = "netlify/functions"

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    X-Frame-Options = "DENY"
